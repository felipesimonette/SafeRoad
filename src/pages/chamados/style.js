import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  unauthorizedContainer: {
    backgroundColor: 'rgba(0,0,0,0.8)',
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  unauthorizedText: {color: '#fff', fontSize: 20, marginBottom: 20},
});
